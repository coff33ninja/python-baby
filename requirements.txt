# Core ML and Data Handling
torch>=1.13.0,<2.0.0 # PyTorch - specify a range for flexibility
transformers>=4.28.0,<5.0.0
datasets>=2.12.0,<3.0.0
evaluate>=0.4.0,<0.5.0 # For metrics like BLEU, ROUGE

# Web API (for master_key.py and potentially other services)
fastapi>=0.95.0,<0.110.0
uvicorn[standard]>=0.22.0,<0.24.0 # Includes standard dependencies like websockets

# Web Scraping
requests>=2.28.0,<2.32.0
Scrapy>=2.8.0,<2.11.0
scrapy-playwright>=0.0.25,<0.0.35 # Manages Playwright integration for Scrapy
playwright>=1.30.0,<1.40.0 # Playwright browser automation library itself

# GUI
gradio>=3.35.0,<4.0.0

# PDF Processing (Optional for GUI uploads)
PyPDF2>=3.0.0,<3.1.0

# Secure Code Execution (for evaluation suite)
RestrictedPython>=5.0,<7.0

# Testing
pytest>=7.0.0,<7.5.0
